services:
  ai-converter:
    # image is pushed by CI (GHCR); server only needs `docker compose pull`
    image: ghcr.io/syahrulrzk/ai-convextract:latest
    build: .
    ports:
      - "3100:3100"
    environment:
      - NODE_ENV=production
      - PORT=3100
      - LOG_LEVEL=info
